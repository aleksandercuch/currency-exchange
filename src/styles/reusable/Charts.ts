import styled from 'styled-components';

export const StyledLineChartInnerContainer = styled.div`
    min-width: 1000px;
    max-width: 100%;
`;

export const StyledLineChartOuterContainer = styled.div`
    overflow-y: hidden;
    margin: 10px 0;
`;