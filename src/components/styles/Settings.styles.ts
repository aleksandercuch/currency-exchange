import styled from 'styled-components';

// This could be stored in global styles component file if we want to all labels in the app share this styles
export const StyledLabel = styled.div`
    display: flex;
    align-items: center;
    gap: 10px;
`;